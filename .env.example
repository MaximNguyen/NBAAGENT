# ===================================================================
# NBA Betting Agent - Environment Configuration
# ===================================================================

# -------------------------------------------------------------------
# Required: External API Keys
# -------------------------------------------------------------------

# The Odds API - Get your free API key at https://the-odds-api.com/
# Free tier includes 500 requests/month
ODDS_API_KEY=your_odds_api_key_here

# -------------------------------------------------------------------
# Display Settings
# -------------------------------------------------------------------

# Default minimum EV threshold for displaying bets
# Examples:
#   0.02 = Show only bets with +2% EV or higher (default)
#   0.05 = Show only bets with +5% EV or higher
#   0    = Show all bets with 0% EV or higher
#  -1    = Show ALL bets including negative EV
DEFAULT_MIN_EV=-1

# -------------------------------------------------------------------
# LLM Provider Configuration
# -------------------------------------------------------------------

# LLM provider for matchup analysis: "anthropic" (default) or "ollama"
# Use "ollama" for free local testing, "anthropic" for production quality
# LLM_PROVIDER=anthropic

# Anthropic API - Claude LLM for production matchup analysis
# Get your API key at https://console.anthropic.com/
# Required when LLM_PROVIDER=anthropic (default)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Ollama Configuration (for free local testing)
# Install Ollama from https://ollama.ai and run: ollama pull llama3.1
# OLLAMA_MODEL=llama3.1
# OLLAMA_HOST=http://localhost:11434

# -------------------------------------------------------------------
# ML Model Configuration
# -------------------------------------------------------------------

# Enable/disable ML-based probability estimation
# When enabled, uses LightGBM model trained on historical data
# ML_ENABLED=true

# Path to the trained ML model (without extension)
# Model will be trained automatically on first run if not found
# ML_MODEL_PATH=.models/moneyline

# Weight of ML model in probability blending (0-1)
# Higher = more weight to ML model, lower = more weight to market odds
# Default: 0.7 (70% model, 30% market)
# ML_MODEL_WEIGHT=0.7

# -------------------------------------------------------------------
# Optional: LangSmith Observability (Disabled by default)
# -------------------------------------------------------------------

# LangSmith provides production tracing and debugging for LangGraph workflows
# Get your API key at https://smith.langchain.com/

# Set to 'true' to enable workflow tracing (opt-in)
# When enabled, all LangGraph executions are traced to LangSmith cloud
# LANGSMITH_TRACING=false

# Your LangSmith API key (required if tracing enabled)
# LANGSMITH_API_KEY=your_langsmith_api_key_here

# LangSmith project name (defaults to "nba-betting-agent")
# LANGSMITH_PROJECT=nba-betting-agent

# LangSmith API endpoint (defaults to https://api.smith.langchain.com)
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
